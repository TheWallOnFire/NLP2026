sai rồi, hãy sửa lại screen detection như sau theo cấu trúc sau từ trên xuống: đàu tiên trên cùng là 1 thanh option về các button dropdown: chọn mode (Live/Image/Video), chọn model. Bên dưới là giao diện màn hình camera, bên trái màn hình là các nút bấm liên quan tới mode hiện tại theo vertical (mode camera thì hiện các button như đảo camera, flash, countdown,... mode image/video thì button mở thư mục để chọn file), Tiếp theo ở đưới đó sẽ có bảng chứa kết quả nhận diện mới nhất. Tiếp theo là một loạt các button để mở các dialog chứa thông tin (button history cho xem dialog lịch sử detect, button pending chứa các ảnh đang chờ xử lý)
bổ xung: ở mode live mất đi button speed rồi, hãy bổ xung vào thanh bên trái màn hình ở đầu danh sách các nút, button speed này sẽ dropdown ra 3 mode: slow, normal, fast, off, tương ứng với độ trễ 2s, 1s, 0.5s và không delay tương ứng với 4 icon tương ứng.